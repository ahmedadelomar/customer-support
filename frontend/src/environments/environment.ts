export const environment = {
  production: false,
  /**
   * Local API. Matches the `applicationUrl` http profile in
   * backend/src/CustomerSupport.Api/Properties/launchSettings.json.
   * The API allows this origin via Cors:AllowedOrigins in appsettings.json.
   */
  apiUrl: 'http://localhost:5256',
};
